<Page x:Class="FluentFlyoutWPF.Pages.IslandPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:controls="clr-namespace:MicaWPF.Controls;assembly=MicaWPF"
      xmlns:viewModels="clr-namespace:FluentFlyoutWPF.ViewModels"
      mc:Ignorable="d"
      d:DesignHeight="800" d:DesignWidth="800"
      Title="IslandPage"
      d:DataContext="{d:DesignInstance viewModels:UserSettings}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      d:Background="White">

    <StackPanel MaxWidth="1000" Margin="16">
        <TextBlock Text="Fluent Island" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,8" />
        <TextBlock Text="Isla superior central con lo que está sonando. Acerca el ratón para ver detalles y controlar la reproducción." FontSize="14" Opacity="0.75" Margin="0,0,0,20" MaxWidth="600" HorizontalAlignment="Left" TextWrapping="WrapWithOverflow" />
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <ui:CardControl x:Name="IslandEnabledCard" Tag="Indexable" Grid.Row="0" Icon="{ui:SymbolIcon Album24}" Margin="0,0,0,24">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Habilitar Fluent Island" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="Muestra la isla arriba al centro mientras suena música." FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch IsChecked="{Binding IslandEnabled, Mode=TwoWay}" />
            </ui:CardControl>
            <ui:CardControl x:Name="IslandModeCard" Tag="Indexable" Grid.Row="1" Icon="{ui:SymbolIcon Timer24}" Margin="0,0,0,3" IsEnabled="{Binding IslandEnabled}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Visibilidad" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="Visible mientras suena, o aviso temporal de 4 s al pausar." FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ComboBox SelectedIndex="{Binding IslandVisibilityMode, Mode=TwoWay}">
                    <ComboBoxItem Content="Visible mientras suena" />
                    <ComboBoxItem Content="Aviso temporal" />
                </ComboBox>
            </ui:CardControl>
            <ui:CardControl x:Name="IslandLineCard" Tag="Indexable" Grid.Row="2" Icon="{ui:SymbolIcon EyeLines24}" Margin="0,0,0,24" IsEnabled="{Binding IslandEnabled}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Línea gris del Island" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="Indica que el Island está activo." FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch IsChecked="{Binding IslandActivityLine, Mode=TwoWay}" />
            </ui:CardControl>
            <ui:CardControl x:Name="IslandEqEnabledCard" Tag="Indexable" Grid.Row="3" Icon="{ui:SymbolIcon SoundWaveCircle24}" Margin="0,0,0,3" IsEnabled="{Binding IslandEnabled}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Ecualizador" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="Barras con el audio real en la isla compacta y expandida." FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ui:ToggleSwitch IsChecked="{Binding IslandEqEnabled, Mode=TwoWay}" />
            </ui:CardControl>
            <ui:CardControl x:Name="IslandEqBarsCard" Tag="Indexable" Grid.Row="4" Icon="{ui:SymbolIcon DataBarVertical24, Filled=True}" Margin="0,0,0,3" IsEnabled="{Binding IslandEqEnabled}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Número de barras" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal">
                    <Slider Minimum="1" Maximum="10" Value="{Binding IslandEqBarCount, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" Width="150" IsMoveToPointEnabled="True" />
                    <ui:TextBlock Text="{Binding IslandEqBarCount}" Width="24" TextAlignment="Center" Margin="12,0,0,0" />
                </StackPanel>
            </ui:CardControl>
            <ui:CardControl x:Name="IslandEqSensitivityCard" Tag="Indexable" Grid.Row="5" Icon="{ui:SymbolIcon BezierCurveSquare20}" Margin="0,0,0,3" IsEnabled="{Binding IslandEqEnabled}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Sensibilidad" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal">
                    <Slider Minimum="1" Maximum="3" Value="{Binding IslandEqSensitivity, Mode=TwoWay}" TickFrequency="1" TickPlacement="Both" IsSnapToTickEnabled="True" Width="150" Margin="0,0,0,0" IsMoveToPointEnabled="True" />
                </StackPanel>
            </ui:CardControl>
            <ui:CardControl x:Name="IslandEqSmoothingCard" Tag="Indexable" Grid.Row="6" Icon="{ui:SymbolIcon SlideTransition24}" Margin="0,0,0,3" IsEnabled="{Binding IslandEqEnabled}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Suavizado" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal">
                    <Slider Minimum="0" Maximum="100" Value="{Binding IslandEqSmoothing, Mode=TwoWay}" TickFrequency="5" IsSnapToTickEnabled="True" Width="150" IsMoveToPointEnabled="True" />
                    <ui:TextBlock Text="{Binding IslandEqSmoothing}" Width="32" TextAlignment="Center" Margin="12,0,0,0" />
                </StackPanel>
            </ui:CardControl>
            <ui:CardControl x:Name="IslandStyleCard" Tag="Indexable" Grid.Row="7" Icon="{ui:SymbolIcon ExtendedDock24}" Margin="0,0,0,3" IsEnabled="{Binding IslandEnabled}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Estilo" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="Notch: la isla sale del borde superior como la referencia." FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <ComboBox SelectedIndex="{Binding IslandStyle, Mode=TwoWay}">
                    <ComboBoxItem Content="Isla flotante" />
                    <ComboBoxItem Content="Notch superior" />
                </ComboBox>
            </ui:CardControl>
            <ui:CardControl x:Name="IslandToleranceCard" Tag="Indexable" Grid.Row="8" Icon="{ui:SymbolIcon CursorHover24}" Margin="0,0,0,76" IsEnabled="{Binding IslandEnabled}">
                <ui:CardControl.Header>
                    <StackPanel Orientation="Vertical">
                        <TextBlock Text="Tolerancia del hover" FontSize="14" FontWeight="Regular" VerticalAlignment="Center" />
                        <TextBlock Text="Alto en píxeles de la zona invisible que detecta el ratón." FontSize="12" Opacity="0.5" />
                    </StackPanel>
                </ui:CardControl.Header>
                <StackPanel Orientation="Horizontal">
                    <Slider Minimum="4" Maximum="30" Value="{Binding IslandHoverTolerance, Mode=TwoWay}" TickFrequency="1" IsSnapToTickEnabled="True" Width="150" IsMoveToPointEnabled="True" />
                    <ui:TextBlock Text="{Binding IslandHoverTolerance}" Width="32" TextAlignment="Center" Margin="12,0,0,0" />
                </StackPanel>
            </ui:CardControl>
        </Grid>
    </StackPanel>
</Page>
